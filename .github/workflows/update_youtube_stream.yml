name: Update YouTube Stream Description

on:
  schedule:
    - cron: '0 * * * *' # Run every hour
  workflow_run:
    workflows: ["Generate YouTube Description"]
    types:
      - completed
  push:
    paths:
      - 'Youtube Descriptions/timings.txt'
  workflow_dispatch:
    inputs:
      force_update:
        description: 'Force update even on or after the service day'
        required: false
        type: boolean
        default: false

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  update-youtube:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install google-api-python-client google-auth-httplib2 google-auth-oauthlib python-dateutil pytz

      - name: Run update script
        env:
          YOUTUBE_CREDENTIALS_JSON: ${{ secrets.YOUTUBE_CREDENTIALS_JSON }}
          FORCE_UPDATE: ${{ github.event.inputs.force_update }}
        run: cd "Youtube Descriptions" && python update_youtube_stream.py
